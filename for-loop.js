// from JavaScript Patterns by Stefanov

// if the array does not change when iterated, 
// there is no need to recompute its length every iteration
for (var i = 0, max = myarray.length; i < max; i++) {
    // do something with myarray[i]
}
